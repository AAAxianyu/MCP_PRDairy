# GitHub PR MCP Server 依赖包
# 基于 Gradio 和 Flask 的 MCP 服务器，支持 GitHub Webhook 处理和 AI 分析

# 核心 MCP 服务器依赖
gradio[mcp]==5.39.0
mcp==1.10.1

# Web 框架
flask==3.0.3

# OpenAI API 客户端
openai==1.93.0

# HTTP 请求库
requests==2.32.3

# 环境变量管理
python-dotenv==1.1.1

# 可选依赖（用于高级功能）
# PyGithub==2.1.1  # GitHub API 客户端（可选）
# selenium==4.18.1  # 浏览器自动化（可选） 